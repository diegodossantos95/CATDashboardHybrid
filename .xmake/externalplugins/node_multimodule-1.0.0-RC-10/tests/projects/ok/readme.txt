This project has:
  - a correct structure,
  - a proper .node_multimodule.json (or .node_multimodule.cfg) configuration file,
  - the correct package.json in the appropriate locations.
  
This project doesn't have a path2 folder, yet it still works because the tgz phase is not triggered.